version: '3'
services:
  db:
   image: mysql/mysql-server:latest
   container_name: mysql
   volumes:
      - mysql:/var/lib/mysql
      - ./SQLFiles/setup-docker.sql:/docker-entrypoint-initdb.d/setup-docker.sql
      - ./config/mysql-config.cnf:/etc/mysql/conf.d/mysql-config.cnf
   env_file:
     - './env/.env.dev.db'
   ports:
     - "3306:3306"
   healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      timeout: 30s
      retries: 10
  web:
    build: 
      context: ./
      dockerfile: Dockerfile.dev
    container_name: web
    links:
      - db
    volumes:
      - ./data/gitpulls:/tmp/gitpulls
      - ./data/logs:/tmp/log
      - ./config:/config
      - ./src/public_html:/home/web/public_html   
    env_file:
      - './env/.env.dev.web'
    ports:
      - "8080:80"
    depends_on:
      - db

volumes:
  mysql:
